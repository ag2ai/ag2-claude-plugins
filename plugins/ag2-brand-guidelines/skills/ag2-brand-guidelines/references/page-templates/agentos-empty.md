# AgentOS — Empty State (Figma `239:2072`)

Same shell as `agentos-chat.md` with no content.

```
[Title Bar — same as Chat View]
[Sidebar — same, but "Twins" is active (blue highlight)]
[Content Area — empty, dark bg only]
[Alt bar — same decorative bottom edge]
```
